name: "Destroy workspace"

on:
  push:
    branches:
    - main
#   schedule:
     # Runs "At 20:00 on every day-of-week from Monday through Friday"
    # - cron: '0 20 * * 1-5'   
  workflow_dispatch:
    inputs:
      terraform_workspace_name:
        description: 'Terraform Workspace Name'
        required: true

permissions:
  id-token: write
  contents: read

env: 
  terraform_actions_working_dir: ${{ github.workspace }}/terraform/

jobs:
  quick_deploy_destroy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

    #   - name: Destroy All Terraform Workspace 
    #     run: |
    #         for i in $(terraform workspace list); 
    #         do 
    #         terraform workspace select $i; terraform destroy -auto-approve;
    #         done
    #     shell: bash
    #     env: 
    #       TF_WORKING_DIR: ${{ env.terraform_actions_working_dir }}
    #       TF_ACTION_WORKSPACE_NAME: ${{ github.event.inputs.terraform_workspace_name }}

      - name: ls
        run: |
            terraform workspace list
        shell: bash
        env: 
          TF_WORKING_DIR: ${{ env.terraform_actions_working_dir }}
          TF_ACTION_WORKSPACE_NAME: ${{ github.event.inputs.terraform_workspace_name }}

