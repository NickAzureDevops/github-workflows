<<<<<<< HEAD
name: 080-resusable-workflow-caller
=======
name: deploy
>>>>>>> main

on:
  push:
    branches: none # [ main ]
  pull_request:
    branches: none # [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  call-workflow-A:
    uses: CloudwithNicholas/github-workflows/.github/workflows/github_linter.yml@reuseable-workflow 
    with:
      username: CloudwithNicholas
    secrets:
      token: ${{ secrets.TOKEN }}

  call-workflow-B:
    uses: CloudwithNicholas/github-workflows/.github/workflows/github_linter.yml@reuseable-workflow 
    with:
      username: CloudwithNicholas
    secrets:
      token: ${{ secrets.TOKEN }}